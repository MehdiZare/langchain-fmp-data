name: Test-Matrix

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python:
          - "3.10"
          - "3.11"
          - "3.12"
          - "3.13"
          - "3.14"

    steps:
      - name: 🛎️  Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: 🐍  Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python }}
          allow-prereleases: true

      - name: ⚡  Install uv (with cache)
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: ▶️  Run Tests
        env:
          CI: "true"
        run: |
          # Install project with test dependencies
          uv pip install --system -e ".[test]"

          # Run tests
          echo "Running tests for Python ${{ matrix.python }}"
          pytest -v

      - name: 🔍 Quality Gates
        if: matrix.python == '3.12'
        run: |
          # Install dev dependencies for linting
          uv pip install --system -e ".[dev]"

          # Run linting
          ruff check src/langchain_fmp_data/ tests/

          # Run type checking
          mypy src/langchain_fmp_data/

          # Run security checks
          bandit -c pyproject.toml -r src/langchain_fmp_data/

  # Coverage job - run all tests in one job for simplicity
  coverage:
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️  Checkout
        uses: actions/checkout@v6

      - name: 🐍  Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: ⚡  Install uv
        uses: astral-sh/setup-uv@v7

      - name: 📊 Run all tests with coverage
        run: |
          # Install project with test dependencies
          uv pip install --system -e ".[test]"

          # Run tests with coverage and JUnit XML for test analytics
          pytest --cov=src/langchain_fmp_data --cov-report=xml --cov-report=term --junitxml=junit.xml -o junit_family=legacy -v

      - name: 📈 Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: MehdiZare/langchain-fmp-data
          files: coverage.xml
          flags: unittests
          fail_ci_if_error: false

      - name: 📊 Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  test-matrix-expected:
    name: Test-MatrixExpected
    runs-on: ubuntu-latest
    needs: [tests, coverage]
    if: ${{ always() }}
    steps:
      - name: ✅ Verify matrix results
        run: |
          if [[ "${{ needs.tests.result }}" != "success" || "${{ needs.coverage.result }}" != "success" ]]; then
            echo "Upstream checks failed"
            exit 1
          fi
          echo "All required checks passed"
